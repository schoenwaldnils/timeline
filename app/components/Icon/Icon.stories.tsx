import { FC } from 'react'

import { Icon as IconComponent } from './Icon'

export default {
  title: 'Icon',
  component: IconComponent,
}

export const Icon: FC = () => <IconComponent icon="search" />
