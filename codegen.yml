schema:
  - ? https://graphql.contentful.com/content/v1/spaces/${CONTENTFUL_SPACE_ID}/environments/${CONTENTFUL_ENVIRONMENT}
    : headers:
        Authorization: 'Bearer ${CONTENTFUL_DELIVERY_TOKEN}'
generates:
  src/@types/generated/contentful.d.ts:
    plugins:
      - typescript
  # src/:
  #   documents: 'src/**/*.gql'
  #   preset: near-operation-file
  #   presetConfig:
  #     extension: .generated.d.ts
  #     baseTypesPath: '@types/generated/contentful.d.ts'
  #   plugins:
  #     - typescript-operations
  #   config:
  #     maybeValue: 'T'
hooks:
  afterAllFileWrite:
    - prettier --write
